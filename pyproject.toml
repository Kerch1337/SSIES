[project]
name = "ssies"
version = "0.1.4"
description = "Library for parsing data SSIES detector DMSP project"
readme = "README.md"
authors = [{name="Kirill"}]
requires-python = ">=3.8"

dependencies = [
    "numpy",
    "pandas",
    "xarray",
    "netCDF4"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

